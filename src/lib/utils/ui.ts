// Shared Burgundy control styles, so inputs/selects across every form stay
// visually identical without repeating long class strings.
export const inputClass =
	'h-10 w-full rounded-md border border-border bg-page px-3 text-[13px] text-primary outline-none focus:border-accent focus:ring-2 focus:ring-accent/30 disabled:opacity-60';

export const labelClass = 'text-[10px] font-bold uppercase tracking-[0.16em] text-muted';
